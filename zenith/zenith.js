const menu=document.querySelector('.menu');const drawer=document.querySelector('.drawer');const close=document.querySelector('.close');const snap=document.querySelector('.snap');const screens=[...document.querySelectorAll('.screen')];
function setDrawer(open){if(!drawer)return;drawer.classList.toggle('open',open);drawer.setAttribute('aria-hidden',String(!open));if(menu)menu.setAttribute('aria-expanded',String(open));document.body.classList.toggle('menu-open',open)}
if(menu){menu.setAttribute('aria-expanded','false');menu.addEventListener('click',()=>setDrawer(true))}if(close)close.addEventListener('click',()=>setDrawer(false));if(drawer)drawer.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>setDrawer(false)));document.addEventListener('keydown',e=>{if(e.key==='Escape')setDrawer(false)});
let settleTimer=null;let settling=false;function settleToNearest(){if(!snap||!screens.length||settling||(drawer&&drawer.classList.contains('open')))return;const top=snap.scrollTop;let nearest=screens[0];let distance=Math.abs(nearest.offsetTop-top);for(const screen of screens){const d=Math.abs(screen.offsetTop-top);if(d<distance){nearest=screen;distance=d}}if(distance<2)return;settling=true;snap.scrollTo({top:nearest.offsetTop,behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});window.setTimeout(()=>{settling=false},360)}
if(snap){snap.addEventListener('scroll',()=>{if(settling)return;clearTimeout(settleTimer);settleTimer=window.setTimeout(settleToNearest,170)},{passive:true});snap.addEventListener('touchend',()=>{clearTimeout(settleTimer);settleTimer=window.setTimeout(settleToNearest,130)},{passive:true})}
const current=(location.pathname.split('/').pop()||'index.html').toLowerCase();if(drawer){drawer.querySelectorAll('a').forEach(a=>{const href=(a.getAttribute('href')||'').split('/').pop().toLowerCase();const home=(current==='index.html'||current==='')&&(href===''||href==='index.html');if(home||href===current){a.classList.add('active');a.setAttribute('aria-current','page')}})}