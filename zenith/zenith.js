const menu=document.querySelector('.menu');const drawer=document.querySelector('.drawer');const close=document.querySelector('.close');const snap=document.querySelector('.snap');const screens=[...document.querySelectorAll('.screen')];function setDrawer(open){drawer.classList.toggle('open',open);drawer.setAttribute('aria-hidden',String(!open));document.body.style.overflow=open?'hidden':''}menu.addEventListener('click',()=>setDrawer(true));close.addEventListener('click',()=>setDrawer(false));drawer.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>setDrawer(false)));document.addEventListener('keydown',e=>{if(e.key==='Escape')setDrawer(false)});let settleTimer=null;let settling=false;function settleToNearest(){if(!snap||!screens.length||settling||drawer.classList.contains('open'))return;const top=snap.scrollTop;let nearest=screens[0];let distance=Math.abs(nearest.offsetTop-top);for(const screen of screens){const d=Math.abs(screen.offsetTop-top);if(d<distance){nearest=screen;distance=d}}if(distance<2)return;settling=true;snap.scrollTo({top:nearest.offsetTop,behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});window.setTimeout(()=>{settling=false},420)}if(snap){snap.addEventListener('scroll',()=>{if(settling)return;clearTimeout(settleTimer);settleTimer=window.setTimeout(settleToNearest,130)},{passive:true});snap.addEventListener('touchend',()=>{clearTimeout(settleTimer);settleTimer=window.setTimeout(settleToNearest,90)},{passive:true})}